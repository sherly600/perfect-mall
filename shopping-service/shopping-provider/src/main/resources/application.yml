spring:
  profiles:
    active: dev
mybatis:
  mapper-locations: classpath*:com/pmall/shopping/dal/persistence/*Mapper.xml"
  type-aliases-package: com.pmall.shopping.dal.entitys
pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql
